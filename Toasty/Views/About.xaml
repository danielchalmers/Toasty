<Window x:Class="Toasty.Views.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:classes="clr-namespace:Toasty.Classes"
        xmlns:helpers="clr-namespace:Toasty.Helpers"
        xmlns:viewModels="clr-namespace:Toasty.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Window.DataContext>
        <viewModels:AboutViewModel />
    </Window.DataContext>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="15" />
            <RowDefinition Height="*" />
            <RowDefinition Height="15" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Text="{x:Static classes:AssemblyInfo.Title}"
                   FontSize="32" />
        <TextBox Grid.Row="2"
                 Text="{x:Static helpers:AboutHelper.AboutText}"
                 Style="{StaticResource InfoTextBox}"
                 VerticalAlignment="Center" />
        <Button Grid.Row="4"
                Content="_Licenses"
                Command="{Binding ViewLicenses}"
                Width="75"
                Height="23"
                HorizontalAlignment="Left" />
        <Button Grid.Row="4"
                Content="_OK"
                Click="btnOK_OnClick"
                IsDefault="True"
                IsCancel="True"
                Width="75"
                Height="23"
                HorizontalAlignment="Right" />
    </Grid>
</Window>